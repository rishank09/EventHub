<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Reset Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-indigo-950 via-zinc-900 to-purple-900 min-h-screen flex items-center justify-center text-white font-sans">
  <div class="w-full max-w-md p-10 bg-white/10 backdrop-blur-md border border-white/20 rounded-3xl shadow-2xl animate-fadeIn">
    <div class="text-center mb-6">
      <h1 class="text-3xl font-bold">Reset Your Password 🔐</h1>
      <p class="text-white/70 mt-2 text-sm">Enter a new password for your account</p>
    </div>

    <form action="/reset-password/<%= userid %>" method="POST" class="space-y-5">
      <input
        type="password"
        name="newPassword"
        placeholder="New password"
        required
        class="w-full px-4 py-3 rounded-xl bg-white/10 border border-white/30 placeholder-white/70 text-white focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
      />
      <button
        type="submit"
        class="w-full py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-xl shadow-lg hover:shadow-blue-500/40 transition"
      >
        Update Password
      </button>
    </form>
  </div>
</body>
</html>
